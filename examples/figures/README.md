# Example figures

This directory contains representative figures produced by the analysis scripts.

The figures are kept separately from `scripts/` so that the script directories contain analysis code only.

Current example figures:

- RDF example
- Hydrogen-bond example
- Hydrogen-bond lifetime example
- MSD example

No CSV output files are included in the repository.
