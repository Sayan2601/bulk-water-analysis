# MSD Analysis

This directory contains scripts for calculating the mean squared displacement (MSD) of bulk water using molecular dynamics trajectories.

## Available Scripts

- `msd_parallel.py` – Calculates the mean squared displacement using multiple time-origin averaging with parallel processing.
